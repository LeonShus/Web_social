import preloader from '../../../assets/images/loading49.gif'

let Preloader = (props) => {
    return (
        <div>
            <img src={preloader}></img>
        </div>
    )
}

export default Preloader